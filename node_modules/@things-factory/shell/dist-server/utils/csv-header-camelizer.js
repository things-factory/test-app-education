"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const csvtojson_1 = __importDefault(require("csvtojson"));
const lodash_1 = __importDefault(require("lodash"));
async function csvHeaderCamelizer(path) {
    return await csvtojson_1.default()
        .preFileLine((line, index) => {
        if (index === 0) {
            return line
                .split(',')
                .map(header => lodash_1.default.camelCase(header))
                .join();
        }
        else {
            return line;
        }
    })
        .fromFile(path);
}
exports.csvHeaderCamelizer = csvHeaderCamelizer;
//# sourceMappingURL=csv-header-camelizer.js.map